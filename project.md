# project.md — 单机版贪吃蛇（raylib + C）

## 1. 项目概述

用 **C 语言 + raylib** 实现一个单机 PC 贪吃蛇小游戏，核心数据用**带哨兵头结点的双向循环链表**表示蛇身，兼作链表学习案例。逻辑与渲染严格分层，逻辑层零 raylib 依赖以便 gtest 单元测试。

### 目标
- 交付一个**好玩有趣、可玩性完整**的贪吃蛇（霓虹暗黑风）。
- 通过真实游戏场景理解**双向循环链表**的增删改查（尾插头删、遍历、判重）。
- 全程分阶段：**界面阶段人工确认、逻辑阶段测试先审后跑、界面与逻辑交替推进**。

### 非目标（Out of Scope）
- 联机/多人、移动端、关卡编辑器、网络排行榜。
- 3D 渲染、复杂美术资源（无外部图片/音频文件）。

---

## 2. 需求规格

### 2.1 玩法规则
| 项 | 规格 |
|---|---|
| 移动模型 | 网格 64×48 逻辑步进 + 像素插值动画；蛇身每节占 2×2 格、食物/道具占 1 格 |
| 死亡条件 | 撞墙、撞内部障碍（**撞到自己身体不再死亡，可覆盖自己**） |
| 障碍 | 初始 0 个；每升 3 级 +1，上限 15；生成避开蛇身/食物/道具/出生区 |
| 边界 | 不穿墙（撞墙死） |
| 普通食物 | 始终存在 1 个；吃到长度 +1、分数 +10 |
| 拾取判定 | **视觉覆盖即拾取**：蛇头 2×2 视觉方块覆盖到目标格（3×3 邻域）即可吃到食物/道具 |
| 道具种类 | 加速、减速、缩短、护盾 |
| 道具数量 | 同时最多 2 个；每 6~10s 随机刷新 1 个（未满时）；出现后 8s 消失 |
| 道具效果 | 加速/减速/护盾各持续 5s；缩短立即 −3 节（最短不少于 3 节） |
| 加速/减速幅度 | 加速：步进 ×0.6；减速：步进 ×1.6；同时生效近似抵消；有效步进限幅 [40, 300] ms |
| 护盾语义 | 5s 内免疫死亡一次，触发后消耗（触发时本步原地不动） |
| 效果叠加 | 重复吃到同类效果 → **刷新持续时间**（不叠层） |
| 等级成长 | 每吃 8 个食物升 1 级 |
| 速度 | 起始 75ms/格，每级 −4ms，下限 35ms（按细格等比折算，保持原像素速度） |
| 模式 | 无尽模式，无胜利条件；速度有下限 |
| 转向 | 允许反向（可覆盖自己往回走）；输入缓冲队列（最多缓存 2 次转向），快速连转不丢输入 |
| 存档 | 最高分持久化到本地文件 `highscore.dat` |
| 变色 | 吃到道具后蛇身**永久**变为该道具的颜色，直到再次吃到其它道具；重开恢复默认 |

### 2.2 操作与流程
- 方向：方向键 / WASD；暂停：P；重开：R；退出：Esc；全屏切换：F11。
- 无独立开始菜单；启动直接开局；暂停/结算以**覆盖层**显示（含本局分数、最高分、操作提示）。
- 游戏结束时提供**可鼠标点击的「RESTART」与「QUIT」按钮**（R/Esc 快捷键同样有效）。
- 窗口支持**放大与全屏**：渲染按固定虚拟分辨率 1120×840 绘制后等比缩放到窗口，布局与比例保持不变。

### 2.3 视觉规格（霓虹暗黑风）
- 窗口 1120×840；网格 64×48（每格 15px，细密网格），游戏区 960×720；顶部 60px HUD（分数、最高分、等级、当前效果倒计时）。
- 蛇：圆角体节、头大尾细渐变、蛇头带朝向眼睛、霓虹发光描边；**蛇身颜色随最近拾取的道具变化**。
- 普通食物：青色圆点；加速：黄色三角；减速：蓝色圆环；缩短：紫红双向箭头；护盾：白色六边形（形状 + 颜色双重区分，色盲友好）。
- 背景深色 + 淡网格线。

### 2.4 音频
- 仅**代码合成音效**（吃食物、吃道具、升级、死亡），无 BGM，无外部音频文件。

### 2.5 非功能需求
- 逻辑层纯 C、零 raylib 依赖，可独立编译与测试。
- 随机数使用**可注入 seed 的自实现 PRNG**，保证测试可复现。
- 所有代码含**详细中文注释**（模块职责、函数入参/出参/返回值、关键算法说明）。

---

## 3. 架构设计

### 3.1 分层架构图
```
┌─────────────────────────────────────────────────────────────┐
│                           main.c                            │
│    raylib 初始化 → 主循环(采集输入/累计时间/固定步长驱动)     │
└───────────────┬──────────────────────────────┬──────────────┘
                │ 调用                           │ 调用
                ▼                               ▼
      ┌───────────────────┐          ┌─────────────────────────┐
      │      ui/ 渲染层    │  只读状态 │      core/ 逻辑层        │
      │  render / theme   │◄─────────│  game / list / rng      │
      │  input  / audio   │          │  items / persist        │
      │  (依赖 raylib)     │          │  (纯 C，无 raylib 依赖)  │
      └───────────────────┘          └───────────┬─────────────┘
                                                 │ 链接
                                        ┌────────▼─────────┐
                                        │  tests/ (C++)     │
                                        │  googletest       │
                                        └──────────────────┘
```

### 3.2 核心数据结构：带哨兵的双向循环链表
```
        sentinel (head, 不存数据)
         ┌──────────────────────┐
         │ prev ─► ...          │
         └───┬──────────────▲───┘
             │ next         │ prev
             ▼              │
      ┌───────────┐   ┌───────────┐   ┌───────────┐
      │ 蛇头节     │◄─►│ 身体节     │◄─►│ 蛇尾节     │
      │ cell(x,y) │   │ cell(x,y) │   │ cell(x,y) │
      └───────────┘   └───────────┘   └───────────┘
         ▲                                    │
         └──────────── next 回到 sentinel ─────┘
```
- 每步移动 = **头插一个新头节点 + 删尾节点**（未吃到时）；吃到食物则只头插不删尾 → 长度 +1。
- 哨兵使插入/删除无需处理空表与首尾边界，遍历从 `sentinel->next` 到 `sentinel->prev`。
- 碰撞判重：从蛇头下一节遍历到蛇尾，与目标格比较。

### 3.3 关键结构草案（示意，实现时细化）
```c
/* core/list.h — 双向循环链表（哨兵） */
typedef struct Node {
    int x, y;            /* 逻辑格子坐标 */
    struct Node *prev;
    struct Node *next;
} Node;

/* core/game.h — 游戏状态 */
typedef enum { DIR_UP, DIR_DOWN, DIR_LEFT, DIR_RIGHT } Direction;
typedef enum { PHASE_READY, PHASE_PLAYING, PHASE_PAUSED, PHASE_GAMEOVER } GamePhase;

typedef struct {
    Node *head;                 /* 哨兵 */
    int   length;
    Direction dir, pendingDir;  /* 禁止反向、每步一次输入 */
    int   foodX, foodY;
    /* items[] / obstacles[] / effects[] / score / level / eaten */
    /* speedStepMs / rngState / phase ... */
} GameState;
```

### 3.4 目录结构
```
SnakeGame/
├─ CMakeLists.txt
├─ project.md
├─ README.md
├─ src/
│  ├─ core/        # 纯 C：types / config / list / rng / game / items / persist
│  ├─ ui/          # raylib：theme / render / input / audio
│  └─ main.c
├─ tests/          # C++ / gtest：test_smoke / test_list / test_rng / test_game / test_items / test_persist
└─ assets/         # 预留（当前无外部资源）
```
构建：**CMake + FetchContent** 自动拉取并编译 `raylib` 与 `googletest`。

---

## 4. TODO 清单（分 Phase，界面与逻辑交替 + 双门禁）

> **门禁规则**
> - 🔷 **界面 Phase**：实现完成后运行程序弹出窗口，由你**手动确认**，通过后进入下一 Phase。
> - 🟩 **逻辑 Phase**：先提交该 Phase 的**测试用例**给你审核，确认无误后运行 `gtest`；全绿后进入下一 Phase。
> - 界面与逻辑**交替推进**，便于联动验证。
> - 所有代码必须带详细中文注释。

| Phase | 类型 | 内容 | 产出 | 验证门禁 |
|---|---|---|---|---|
| **P0** | 基建 | CMakeLists + FetchContent(raylib/gtest)、目录结构、注释规范、空窗口 | 可构建工程 | 构建成功 + 空白窗口**人工确认** + gtest 冒烟 |
| **P1** | 🟩 逻辑 | 双向循环链表 ADT：创建/销毁/头插/尾删/长度/遍历/判重（哨兵） | `core/list.*`, `test_list.cpp` | 测试用例先审 → gtest 全绿 |
| **P2** | 🔷 界面 | 窗口/网格/霓虹主题/HUD 静态渲染；用假数据画蛇、食物、道具 | `ui/render.*`, `ui/theme.*` | **人工确认**画面 |
| **P3** | 🟩 逻辑 | 游戏状态与移动：初始蛇、禁止反向、尾插头删步进、撞墙/障碍/自身、吃食物增长、食物生成 | `core/game.*`, `test_game.cpp` | 测试用例先审 → gtest 全绿 |
| **P4** | 🔷 界面 | 蛇圆角渐变+眼睛、食物/道具几何形状、**像素插值动画**、输入映射与暂停 | `ui/render.*`, `ui/input.*` | **人工确认**可操控可玩 |
| **P5** | 🟩 逻辑 | 道具系统（刷新/计时/加速/减速/缩短/护盾/刷新时长）+ 等级提速 + 障碍动态增长 | `core/items.*`, `test_items.cpp` | 测试用例先审 → gtest 全绿 |
| **P6** | 🔷 界面 | 道具特效与 HUD 倒计时、暂停/结算覆盖层、护盾视觉、**合成音效**播放 | `ui/render.*`, `ui/audio.*` | **人工确认** |
| **P7** | 🟩 逻辑 | 最高分读写与存档文件处理 | `core/persist.*`, `test_persist.cpp` | 测试用例先审 → gtest 全绿 |
| **P8** | 🔷 集成 | 联调打磨、玩法平衡、性能检查、完善 README、最终人工验收 | 完整游戏 | **人工验收** |

> **实施记录**：P0–P8 已全部完成。实际交付中追加了以下需求：
> - 转向输入由“每步一次”升级为**输入缓冲队列（≤2 次）**，解决快速连转丢键、不跟手的问题；
> - **允许反向并可覆盖自己**：撞自己身体不再死亡，只有撞墙/撞障碍会死；
> - **蛇身随拾取道具永久变色**：加速=黄、减速=蓝、护盾=白、缩短=紫红，不再变回默认色；
> - **视觉覆盖即拾取**：蛇头 2×2 方块覆盖到目标格（3×3 邻域）即可吃掉食物/道具；
> - 渲染改用垂直同步（不再叠加 SetTargetFPS），提升移动丝滑度；
> - 结算界面提供**可鼠标点击的 RESTART / QUIT 按钮**；
> - 支持**窗口放大与 F11 全屏**（虚拟分辨率缩放，布局不变）；
> - 明确道具速度效果幅度（加速 ×0.6 / 减速 ×1.6，限幅 20~150ms）与护盾触发时“原地不动”；
> - 网格加密到 **64×48（每格 15px）**：蛇每步位移更小、视觉上看不出“跳格”，更丝滑；蛇身每节占 **2×2 格**，食物/道具占 **1 格**；速度按细格等比折算（75/4/35ms）。
>
> 测试规模：6 个测试套件、76 个用例，全部通过。

---

## 5. 测试策略

- 测试框架：**googletest**（CMake FetchContent），测试代码用 C++ 编写，通过 `extern "C"` 链接纯 C 逻辑模块。
- 随机性：`core/rng` 提供可注入 seed 的 PRNG，`GameState` 持有 rng 状态，测试可固定种子复现。
- 重点覆盖：
  - 链表：头插/尾删/长度/遍历顺序/判重/哨兵边界（空表、单节、回环正确性）。
  - 游戏：步进位移、吃到食物增长、未吃时长度不变、三种死亡判定、反向输入被拒、单步入队。
  - 道具：刷新上限与计时、效果时长刷新、缩短下限保护、护盾免死一次后失效。
  - 难度：每 8 食物升级、速度下限、每 3 级障碍 +1 且不超上限、生成避让规则。
  - 存档：最高分读取/更新/写入与文件不存在时的默认值。
- **流程**：每次测试用例先提交审核 → 确认 → 运行 → 全绿方可进入下一 Phase。

---

## 6. 验收标准

1. **构建**：`CMake` 一键 configure + build 成功，自动拉取 raylib 与 googletest，无需手动装库。
2. **分层**：`core/` 不含任何 raylib 头文件；`gtest` 可独立链接并运行。
3. **分阶段门禁**：
   - 每个 🔷 界面 Phase 结束后程序可运行且**由你手动确认**后才进入下一 Phase。
   - 每个 🟩 逻辑 Phase 的测试用例**先经你审核**后运行，gtest 全绿才进入下一 Phase。
   - 界面与逻辑按 P0→P8 **交替**推进，且存在可玩的联动验证节点（P4、P8）。
4. **玩法正确性**：撞墙/撞障碍死亡（**撞自己不死、可覆盖自己、允许反向**）；输入缓冲（≤2 次转向）不丢键；吃食物 +1/+10；每 8 食物升级、速度 150→70ms；每 3 级障碍 +1 且 ≤15；道具 ≤2 个、6~10s 刷新、8s 消失、效果 5s、缩短 −3 且最短 3 节、护盾免死一次；吃到道具蛇身变对应颜色。
5. **视觉**：霓虹暗黑风；细密网格 64×48（每格 15px）、窗口 1120×840、顶部 HUD；蛇为圆角渐变体节（每节 2×2 格）+ 眼睛；食物/道具占 1 格且有形状与颜色区分；障碍为霓虹红块；移动有插值动画。
6. **音频**：吃食物/吃道具/升级/死亡均有代码合成音效，无外部音频文件，无 BGM。
7. **存档**：最高分保存到 `highscore.dat`，重启后保留。
8. **窗口**：支持窗口放大与 F11 全屏，虚拟分辨率缩放保证布局不变；游戏结束有可点击的 RESTART / QUIT 按钮。
9. **代码质量**：所有函数含详细中文注释；命名清晰；无内存泄漏（链表/游戏状态创建与销毁成对）。
10. **文档**：README 说明玩法、操作、构建与运行方式。
